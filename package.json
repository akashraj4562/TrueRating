{
  "name": "truerating",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc --noEmit",
    "build": "node build.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.21.0",
    "happy-dom": "^20.9.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
